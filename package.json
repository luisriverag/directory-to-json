{
  "name": "directory-to-json",
  "version": "1.1.0",
  "description": "Write a representation of a directory to a `JSON` file.",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test tape test.js | tap-bail | tap-spec",
    "test-cov": "NODE_ENV=test istanbul cover test.js",
    "lint": "eslint ."
  },
  "repository": "wercker/directory-to-json",
  "keywords": [
    "directory",
    "JSON",
    "write",
    "analyze",
    "walk"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^0.11.0",
    "istanbul": "^0.3.5",
    "tap-bail": "0.0.0",
    "tap-spec": "^2.1.2",
    "tape": "^3.0.3"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
